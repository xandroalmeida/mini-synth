# Dependências de execução do Mini Synth.
#
# A interface usa pywebview no backend GTK/WebKit: o WebKitGTK e o PyGObject
# (gi) vêm dos pacotes APT do sistema (gir1.2-webkit2-4.1, python3-gi), NÃO do
# pip. Por isso o venv deve ser criado com --system-site-packages (veja o
# postinst do .deb / scripts/install-ubuntu.sh).
pywebview>=5.0
pyfluidsynth>=1.3.3
python-rtmidi>=1.5
PyYAML>=6.0

# Desenvolvimento / testes:
pytest>=8.0
Pillow>=10.0   # só para regenerar o ícone (scripts/make-icon.py)
